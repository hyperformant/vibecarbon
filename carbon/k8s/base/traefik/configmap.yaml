apiVersion: v1
kind: ConfigMap
metadata:
  name: traefik-config
  labels:
    app: vibecarbon-traefik
    component: ingress
data:
  # Additional Traefik configuration can be added here
  # This is used by the Traefik deployment via volume mount if needed
  traefik.yml: |
    # Static configuration (if needed beyond CLI args)
    # Currently most config is passed via CLI args
