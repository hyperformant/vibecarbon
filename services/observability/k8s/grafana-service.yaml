apiVersion: v1
kind: Service
metadata:
  name: grafana
  labels:
    app: vibecarbon-grafana
    component: observability
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: vibecarbon-grafana
