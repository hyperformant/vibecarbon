apiVersion: v1
kind: Service
metadata:
  name: prometheus
  labels:
    app: vibecarbon-prometheus
    component: observability
spec:
  type: ClusterIP
  ports:
    - port: 9090
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: vibecarbon-prometheus
